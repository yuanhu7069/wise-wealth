# A 期沟通记录（a-claude-communication-records.md）

> 记录对象：智策理财 A 期（工程骨架）规划阶段，苑问 × Claude（Claude Code · Fable 5）的全部沟通
> 时间：2026-09-07（单日完成）
> 记录人：Claude　确认人：苑问
> 用途：决策溯源。基线 PRD FAQ Q7 约定「AI 反复问 A/B/C 选哪个的地方要回填基线」——本记录（§三）是回填素材；遗留问题清单（§二）是后续期拆解的直接输入
> 关联文档：prd-a.md / arch-a.md / design-a.md / a-plan.md
> **敏感信息声明**：数据库口令与完整连接串在本记录中一律不复述；唯一落盘位置为 `.env`（已被 `.gitignore` 排除，禁止提交 Git）

---

## 一、沟通时间线

### 1.1 项目导入：PRD 精读（苑问 → Claude）

**苑问**：这是一个个人理财系统，文档见 docs 下，不会一次性完成开发。先熟悉这个非标准的项目文档，有问题或了解了之后告知，会指导拆解每一期的功能开发。

**Claude 动作**：通读 `docs/智策理财_PRD_V1.1.md`（V1.1，约 730 行），项目背景写入长期记忆。

**理解汇报要点**：

- 定位：不碰资金的理财决策工具。「免费层告诉你怎么做，Plus 层告诉你为什么是这个数」——这句话同时是产品边界、商业逻辑和合规底线
- 结构：一套内核（YAML 模式库 + 规则引擎 L1 分账/L2 配置/L3 执行 + DecisionTrace 推理链 + 模拟引擎），两套视图（Guide 免费 / Lab Plus）
- 复访闭环：月度快照（4 个余额数字，30 秒）→ 偏离提醒 → 季度复盘；「月度快照而非逐笔记账」是产品最关键的结构决策之一
- 合规硬约束：输出仅到资产大类、全站不出现产品名/代码/链接、免责声明同屏；《金融产品网络营销管理办法》2026-09-30 施行
- 路线图：Phase 1 MVP → Phase 2 Plus 基础 → Phase 3 Plus 进阶 → Phase 4 生态

**Claude 提出 5 组疑问（含 2 个小疑点）+ 1 项清单确认**，苑问答复「具体拆解时再解决」；全部登记于 §二 #1-#7。

### 1.2 苑问指令：转向 A 期拆解

**苑问**：疑问点了解了，这几个在具体拆解的时候再解决；产品文档也是较粗略的，表达我想做一个什么。现在要求：拆解一个较小的实现（可以是基础项目搭建，或者其他非常小的功能），并基于 `/home/yuanhu/project-baseline-docs` 目录下的基线文档，产出 A 期实例文档（prd-a.md、arch-a.md、design-a.md）。

### 1.3 基线阅读与环境确认（Claude）

**Claude 动作**：通读三份基线文档；检查本机工具链。

**基线要点吸收**：

- PRD 基线 v0.2：13 章节模板 + 附录 A 自检硬门（不全绿不进 Plan）；【AI提示】标注保留不删
- arch 基线 v0.2：技术栈锁定（后端 Rust + axum + sqlx + PostgreSQL；前端 Next.js 16 + React 19 + TS 7 + Tailwind 4 + shadcn + Biome；ESLint 因与 TS7 不兼容被排除）；monorepo 结构；17 条红线；§17 AI 生成代码硬约束
- design 基线 v0.1：Token 体系（主色松墨绿 #4A6B57）、红涨绿跌带符号、组件五态、Level A/B/C 设计投入分级

**环境事实（当时）**：Rust 1.94.0 / Node 22.22.1 / Git 2.43.0；本机当时无 psql（后由苑问安装）。

**A 期定位选择**：Claude 建议选「工程骨架」而非最小业务功能。理由：基线锁定的技术栈含 4 个较新组合（Rust+axum 0.8、Next 16+React 19、TS 7、Tailwind 4+shadcn 4.21），兼容风险必须前置暴露；分层、统一响应信封、Token、类型生成链路若不在地基期定死，后续各期会各写各的。该定位进入三份实例文档，苑问未提出异议。

### 1.4 第一次澄清：中间件不在本机

（Claude 尝试进一步探查 WSL/docker 配置时被苑问中断。）

**苑问**：数据库、缓存中间件等都已经安装好，只是不在本机；本机属于开发机。继续上述任务。

→ 由此确立部署拓扑：本机 = WSL2 开发机；中间件 = 外部实例；连接参数属实例层（arch 基线 §0.0），落 `.env`。

### 1.5 三份实例文档产出（Claude）

产出 `docs/prd-a.md`（REQ-20260907-01）、`docs/arch-a.md`、`docs/design-a.md`，并自查通过 PRD 基线附录 A 自检清单。

**A 期代决策台账**（Claude 代为决策、已写入文档、待苑问复核，可推翻）：

| 决策 | 内容 | 记录位置 |
|------|------|---------|
| A 期免鉴权 | 无业务数据即不引入账号体系；触发条件 = B 期首个业务端点前必须引入 JWT + argon2 | arch-a.md ADR-A-001 |
| /health 语义 | 服务进程存活即 HTTP 200，数据库状态放 `data.db` 字段；可区分「进程不可达」与「库不可达」两种错误态 | ADR-A-003 |
| 备份策略延迟 | A 期无业务表不写空架子脚本；首个含业务数据的迁移合并时强制补 `backup.sh` + 恢复演练 | ADR-A-002 |
| A 期不接缓存 | 中间件虽已部署，无热数据不引入；引入时另写 ADR | ADR-A-004 |
| 免责声明提前落模板 | P01 页脚全量展示产品 PRD §13.1 原文，B 期含收益预测页面直接复用 | design-a.md |
| 文案唯一来源 | P01 文案只以 prd-a.md §8.4/§7.1 为准，design-a.md 不设第二来源 | design-a.md §5 |

**事故记录**：design-a.md 首次生成输出严重损坏（乱码行、重复句、元注释泄漏），当场判定不可用，整体重写为 v0.2 并通过干净性校验（grep 无残留标记）；prd-a.md / arch-a.md 无损坏。

### 1.6 数据库连接信息：归属处理（苑问 → Claude）

**苑问**：提供 psql 连接信息（主机 / 端口 / 用户 / 库名 / 口令），询问「是否也可以放到 arch-a.md 下？」

**Claude 答复**：**不能入 arch-a.md**。依据：arch 基线红线 3「禁止硬编码密钥、数据库 URL、端口」+ arch-a.md 自述「连接参数见 `.env`，不写入本文件」——arch-a.md 要进 Git，凭证写入即等于提交进版本库。正确归属：真实值 → `.env`（gitignore 排除），键名模板 → `.env.example`。

**Claude 动作**：创建 `.env` / `.env.example` / `.gitignore`；口令含 `#`，按连接串规则 URL 编码为 `%23`；验证实例端口可达。

**测试插曲**：本机当时无 psql，改用临时 venv + pg8000 纯 Python 驱动测试。第一次尝试禁用证书校验被 Claude Code 安全机制拦截（拦截合理：不应为测试而降级验证）；改为完整校验方式后测得结论。

**重要发现（RISK-A-1）**：该实例**拒绝 SSL 握手**——数据库口令目前明文过公网。处置：A 期不阻塞（库中无业务数据）；**B 期引入真实财务数据前必须在服务器端启用 TLS 或改走 SSH 隧道**，届时回填 arch-a.md §6。

### 1.7 第二次澄清 + 执行计划（苑问 → Claude）

**苑问**：已安装 postgresql 客户端，可以直接使用 psql 测试了；当前任务：基于上述文档生成执行 a-plan.md。

**Claude 动作**：

- psql 补完 DEP-001 验证：认证通过；服务器为 PostgreSQL 15.19；已有 `wise_wealth_db`（A 期用作 dev 库）与 `testdb`
- 产出 `docs/a-plan.md`：T1 仓库初始化 → T2 Rust 后端骨架 → T3 Next 前端骨架 → T4 类型生成链路 → T5 启动/质量脚本 → T6 验收收尾；串行执行、每任务设完成门；9 条 AC 全部映射到具体任务

### 1.8 本记录（苑问 → Claude）

**苑问**：将上述沟通内容写入独立文档 a-claude-communication-records.md。

---

## 二、遗留问题清单（后续期拆解的直接输入）

| # | 事项 | 提出方 | 约定解决时机 |
|---|------|--------|-------------|
| 1 | YAML 双 remainder 桶（工资/投资账户）无法唯一求解，分账语义需定稿 | Claude | 规则引擎拆解期（苑问：具体拆解时解决） |
| 2 | Q4 采集房贷余额，但产品 PRD §13.3 说房贷 Phase 2 才进引擎——MVP 是只采集不用？ | Claude | 同上 |
| 3 | 问卷 → L2 自动匹配规则 PRD 未定义（仅 `default_L2` 示例） | Claude | 同上（Claude 建议拆解时一起定） |
| 4 | 规则引擎归属：产品 PRD 建议 Python 栈，arch 基线锁定 Rust——**以基线为准** | Claude | 已基本解决；B 期实现时最终确认 |
| 5 | 永久组合再平衡阈值出处核实（PRD 写 ±15%（<10%/>40%），Claude 印象 Browne 原版为 15%/35% 触发） | Claude | 知识库开发期 |
| 6 | localStorage 架构下埋点匿名 ID + 上报通道方案 | Claude | B 期，随首个业务功能 |
| 7 | MVP L1 模式清单确认（四账户 / 标准普尔 / 理财金字塔 / 四笔钱 / 50-30-20 共 5 个） | Claude | Phase 1 拆解期 |
| 8 | RISK-A-1：实例无 TLS，口令明文过公网 | Claude 发现 | **B 期引入真实数据前必须解决** |
| 9 | dev/prod 库分离：当前 `wise_wealth_db` 暂作 dev 库 | Claude 发现 | 引入真实数据前另建正式库（RULE-002 强制） |
| 10 | a-plan.md §0 含实例 IP 明文，首次提交前建议改为「外部实例（连接参数见 .env）」 | Claude | T1 执行时 |

## 三、对基线的回填建议（基线 FAQ Q7 信号）

| 信号 | 建议 |
|------|------|
| arch 基线未定义健康检查响应语义（恒 200 与 5xx 两流派并存） | ADR-A-003 已做项目级决策；若多项目复用，可升入基线 |
| PRD 模板对「无业务功能的工程期」缺少写法指引（成功指标/埋点/合规章如何表达不适用） | A 期实践了「不允许只写不涉及而不给理由」；可回填模板 |
| 凭证经会话传递缺乏操作约定（本次苑问在对话中直接贴口令） | 可在基线加一条：凭证只落 `.env`，任何进 Git 的文档与记录一律不复述 |

## 四、环境事实定格（2026-09-07）

| 项 | 状态 |
|----|------|
| 开发机 | WSL2，Windows 浏览器访问 dev server |
| 工具链 | Rust 1.94.0 / Node 22.22.1 / Git 2.43.0 / psql（苑问安装） |
| PostgreSQL | 15.19，外部云实例，psql 认证通过，无 TLS（RISK-A-1） |
| 仓库状态 | 尚未 `git init`（a-plan.md T1 执行） |
| 已就位文件 | `.env` / `.env.example` / `.gitignore`；`docs/`（产品 PRD + 三实例文档 + a-plan.md + 本记录） |

## 五、产出物清单（按产生顺序）

1. 项目长期记忆（Claude 本地，不在仓库）
2. `docs/a/prd-a.md` v0.1 — A 期 PRD 实例（REQ-20260907-01）
3. `docs/a/arch-a.md` — 架构实例（4 条项目级 ADR）
4. `docs/a/design-a.md` v0.2 — 设计实例（v0.1 损坏，重写）
5. `.env` / `.env.example` / `.gitignore`
6. `docs/a/a-plan.md` — 执行计划（T1-T6）
7. `docs/a/a-claude-communication-records.md` — 本记录

> 目录约定（2026-09-07 苑问指定）：A 期文档统一收在 `docs/a/`；跨期共用文档（产品 PRD）留 `docs/` 根。后续各期照此建 `docs/b/`、`docs/c/`……同批整理时一并处理：a-plan.md 中实例 IP 明文改为「见 .env」、prd-a.md / arch-a.md 三处「局域网可达」过时表述修正为「公网可达（云实例）」、arch-a.md 库名笔误（wise_wealth_dev → 以 .env 为准）。

## 六、当前状态与下一步

**A 期已验收闭环（2026-09-08）**：T1–T6 全过，9/9 AC 确认（其中 AC-7/AC-9 为 playwright 真浏览器断言），苑问目检通过；prd-a.md 状态「已完成」。提交链：`35271cb`（字阶/间距注册修复 + base- 前缀）→ `b8658c1`（Token 样例区 + 免责声明区，遗留项 5 闭环）→ `5db1215`（测试报告升级）→ `ab2ea3f`（验收闭环回填）。

**B 期开工前待办（不阻塞，均已在 a-plan.md §5 尾注登记）**：

- RISK-A-1：外部 PG 实例启用 TLS 或改走 SSH 隧道（引入真实数据前**必须**），届时回填 arch-a.md §6
- TS7 二进制 vendor 工作态核对；openapi-typescript 与之并存的关系说明
- 触控热区 40px → 44px 统一调整（基线可达性要求）
- shadcn CLI 网络受限改手写等价组件的维护说明（README 或 arch 附录）

> 目录约定（2026-09-07 苑问指定）：A 期文档统一收在 `docs/a/`；跨期共用文档（产品 PRD）留 `docs/` 根。后续各期照此建 `docs/b/`、`docs/c/`……同批整理时一并处理：a-plan.md 中实例 IP 明文改为「见 .env」、prd-a.md / arch-a.md 三处「局域网可达」过时表述修正为「公网可达（云实例）」、arch-a.md 库名笔误（wise_wealth_dev → 以 .env 为准）。（以上四项已于 2026-09-08 核对落实：目录结构符合约定，IP/局域网/库名 grep 零残留。）
