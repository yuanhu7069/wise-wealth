# B 期测试报告(test-report-b.md)

> 本文件由 ticket 08 起头(备份与恢复演练一节),其余章节由 ticket 09 补齐:
> AC 回链(prd-v1 §4.2 十七条)、自动化测试结果、基线 §16 审查清单、依赖审计
> (cargo audit / npm audit)、遗留清单。**未写到的章节 = 未做,不预留空表假装已完成。**

## 1. 备份与恢复演练(AC-15 / ADR-A-002 兑现)

### 1.1 备份侧:已通过(2026-09-11)

证据脚本 `.scratch/b-guide-flow/tmp-verify-08.sh` 六节全绿(退出码 0)。

| 验的什么 | 怎么验的 | 结果 |
|---|---|---|
| 转储为自定义格式且档案可读 | `pg_restore --list` 列出 CUSTOM 格式与 5 张业务表 | ✓ |
| 校验文件可用 | `sha256sum -c` 通过;**改动转储 1 字节后如实报错** | ✓ |
| 备份内容含代码版本与脱敏配置(基线 §7.3) | 检查同名 `.meta`:git 版本、已应用迁移数、APP_ENV/APP_PORT 等非密钥项;并断言密钥值不出现在文件里 | ✓ |
| 保留策略:最近 7 个日历日各一份 + 最近 5 个 ISO 周各一份 | 在**临时目录**造 20 天历史(含同一天跑 3 次、窗口外 60 天各一份)后跑清理,按属性断言:最近 7 个日历日各留 1 份、同一天只留最新那份、窗口外 0 份、最老一份距今 ≥28 天 | ✓ 实测 35 天 |
| 备份不进版本库 | `git check-ignore` + `git status --porcelain` | ✓ |
| 运行手册的红线、命令与升级路径 | grep 断言红线原句、备份/演练命令、升级前先备份(2026-09-11 文档拆分:原先断言的 `README.md` 运行内容已移入 `DEPLOY.md`,断言对象随之更换) | ✓ |

**保留窗口为什么是「7 个日历日 + 5 个 ISO 周」**:基线 §7.3 要求至少能回退到 7 天前与 1 个月前。
按「最新 7 个文件」保留时,一天跑多次只覆盖一天多;按 4 个 ISO 周最老只到 22 天 —— 两者都不达标。
故按日历日计数,并把周备从 4 加到 5(实测最老一份 35 天)。

备份文件本身不在此处枚举:它们按保留策略轮转,写进报告那一刻起就在变;下面演练记录里那份是当次演练所用的。

### 1.2 恢复演练:已通过(2026-09-11)

一条命令跑完「备份 → 副本库恢复 → 抽验 → 用副本库起服务」(`scripts/restore-drill.sh`),
全程不写源库(脚本内两道闸:地址改写后必须真的换了库名、且结果不得等于源库)。

| 项 | 值 |
|---|---|
| 时间 | 2026-09-11 00:24 CST |
| 源库 | dev 库 |
| 备份文件 | `backups/wise_wealth_20260911_0024.dump`(该文件随保留策略自然轮转,不影响本记录) |
| SHA256 | `0f99c5d86afad4ddfde2476afac5d4e045db5dd08b2cc9274312ed23910f1046` |
| 副本库 | `wise_wealth_db_test`(与源库同属主;恢复用 `--single-transaction`,要么整份成功要么整份回滚) |
| 抽验结果 | 行数 / 金额合计 / 内容指纹 **共 10 项,全部一致** |
| 恢复后可用性 | 副本库上起服务 → `/health` `"db":"ok"`;用种子账号**真的登录成功**(口令哈希随备份恢复);读出方案数据(第 2 版 / four_accounts) |

抽验比的是**数据本身**,不是「能不能查」——行数一样而金额错位是备份最危险的失效形态。所以除 5 张表
行数外,还比了 profiles 三项金额合计、plan_buckets 两项金额合计、plans 的版本与可投资额,
以及 plans / profiles 的**内容指纹**(`md5(string_agg(...))`)。

**AC-15 结论:通过。** ADR-A-002 至此兑现,`README` 的红线「恢复演练未通过前,不录入不可重建的
真实数据」随之**解除**;该红线在换库、改迁移脚本或改恢复链路后**重新生效**,需重跑演练再录真实数据。

**留一条给将来**:演练比对的是「此刻源库 vs 该备份」。备份生成后若源库仍有写入,行数不一致属预期 ——
那时的看点是「能不能恢复」,不是「等不等」。脚本在演练指定旧备份时会给出这条提示。

**未验到的部分**:`restore-drill.sh` 的 **prod 分支**(`APP_ENV=prod` 时覆盖 `DATABASE_URL_PROD` 起服务)
本次未执行 —— 演练在 dev 库上做的,prod 分支属代码审查覆盖、非实跑覆盖。正式库首次使用前应补一次。

## 2. 走查环境与方法

| 项 | 值 |
|---|---|
| 日期 | 2026-09-11 |
| 代码版本 | 工作区 `247b986`(ticket 01-08)+ 本轮 `README.md` 改动 |
| 服务 | `server`(Rust,`127.0.0.1:8080`)+ `web`(Next,`localhost:3000`) |
| 数据库 | dev 库(远程 PostgreSQL 16) |
| 浏览器 | Chromium headless(Playwright core 1.63.0,`chromium-1208`) |
| 视口 | 320 / 375 / 768 / 1280 |
| 主题 | 亮 / 暗(`colorScheme` 模拟系统偏好) |
| 工具 | `scripts/check.sh`(自动化门禁)+ 一次性走查脚本 |

**走查前置:档案必须是「首次填写态」**(`draft_step=1` / `questionnaire_completed=false`),
否则断点恢复与「无方案空态」两项根本不适用。这次确实踩过一次 —— 先在「已完成档案」上测 AC-6,
测出「重进停在步 6」,查证为**设计预期**(步进永不回退,`profile_service.rs` 内 `next.draft_step =
current.draft_step.max(advanced)`,单测「进度不得回退」锁定),把档案重置后才测到真实行为。

走查脚本为一次性脚本,跑在 `/tmp` 下、**不进仓库**;需要复现时按本节表格重建环境即可。

## 3. AC 回链(prd-v1 §4.2 十七条)

| AC | 结论 | 本期实跑证据 |
|---|---|---|
| AC-1 正常·登录 | ✓ | P02 登录成功并跳转;响应头 `Set-Cookie: ww_session=…; HttpOnly; SameSite=Lax`;无注册入口、无默认口令 |
| AC-2 异常·登录 | ✓ | 口令错 → 表单下「用户名或密码不正确」且不设 Cookie;同一用户名第 1-5 次普通失败、**第 6 次起 429** |
| AC-3 异常·会话过期 | ✓ | 清 Cookie 后访问 P03 → `/login?from=/questionnaire&expired=1` 并提示「登录已过期」;就地重登 → 回 P03,**步号与过期前一致(6 → 6)**,已答内容完整 |
| AC-4 边界·from 校验 | ✓ | `from=//evil.com` 与 `from=https://evil.com` 均被丢弃 → 登录后落 P01 |
| AC-5 正常·金例生成 | ✓ | 按金例 A 端到端跑一遍:四桶 ¥4,500 / ¥3,600 / ¥3,100 / ¥800、必要月支出 ¥8,100、应急目标 ¥97,200、当前 3.0 个月、L2 60/40、约 24 个月补齐 —— **10 项与 §7.1 表逐项一致** |
| AC-6 正常·草稿恢复 | ✓ | 步 3 答完后换新页面重进 → 停在步 4,步 1 选中态保持 |
| AC-7 异常·表单校验 | ✓ | 月收入填 0 → 输入框下「金额需大于 0」,草稿未落库、步进被阻止 |
| AC-8 正常·推荐 | ✓ | 步 6 两张卡(四账户 / 50/30/20)含可信度徽章与一句话理由、按 RULE-006 标主推;两张卡各自都能生成方案 |
| AC-9 边界·短久期 | ✓ | 久期「1 年内」→ L2 现金类 100%,并给出原因句;无权益类比例(原因句本身的文案瑕疵见 §8-D) |
| AC-10 边界·应急已达标 | ✓ | 存款 ¥150,000 → 备用 ¥0.00、投资 ¥3,900.00、「应急金:已达标」+「现有存款已覆盖约 18.5 个月」+「投资账户已有家底 ¥52,800.00」 |
| AC-11 边界·收入不足 | ✓ | 收入 ¥8,000 / 支出 ¥7,000 / 存款 ¥5,000 → 投资 ¥0.00、备用 ¥0.00、「收入不足以覆盖当前结构…」缺口提示;**无任何负数金额** |
| AC-12 正常·版本化 | ✓ | 走查期间方案递增至第 9 版;旧版本留存且 `is_active=false`;P01 摘要跟随最新 active |
| AC-13 合规红线 | ✓ | grep 全部前端文案与后端配置:无产品名称 / 代码 / 管理机构 / 购买链接;唯一外链是 `API_BASE_URL=http://127.0.0.1:8080`(本机后端,非购买入口);L2 配置为资产大类(权益类 / 债券类 / 现金类 / 宽基权益 / 长期债券 / 黄金) |
| AC-14 数据·埋点 | ✓ | SQL 查 `analytics_events`:5 类事件各 ≥1;`plan_generated` 载荷含 `l1_mode` |
| AC-15 数据·备份 | ✓ | 见 §1 |
| AC-16 体验·双端与暗色 | ✓ | 首轮走查的三处缺口(暗色手动切换 / 触控 ≥44px / 顶部栏与登出)已补齐,4 处正文级对比度亦已修,第二轮复验通过 —— 见 §11 |
| AC-17 展示·金额 | ✓ | 方案页 `¥10,000.00` 千分位 + 2 位小数 + `tabular-nums` 右对齐 |

**17 条:全部通过。** AC-16 首轮不通过(§8-A / §8-B / §8-C),补齐后第二轮复验通过(§11);
AC-9 通过,其文案瑕疵已修(§8-D)。

## 4. 自动化测试结果

`scripts/check.sh` 一次跑完,全绿:

| 门禁 | 结果 |
|---|---|
| `cargo clippy -D warnings` | ✓ 无警告 |
| `cargo test` | ✓ **94 passed / 0 failed** |
| `biome check` | ✓ 32 files |
| `tsc --noEmit` | ✓ |
| Token 门禁 | ✓ |

## 5. 人工走查明细

### 5.1 四档 × 四页溢出扫描

320 / 375 / 768 / 1280 × `/`、`/login`、`/questionnaire`、`/plan`,亮暗双主题各扫一遍,
按 `scrollWidth/scrollHeight` vs `clientWidth/clientHeight` 判定:**全部 0px 溢出,零命中**。

### 5.2 对比度与 axe-core 扫描(基线 §16.5 / §14.4)

axe-core(注入 `axe.min.js`,4 页 × 2 视口 × 2 主题共 16 次扫描)结论:**唯一违规类型是
`color-contrast`**,无表单 label 缺失、无 landmark / heading 层级等问题。命中分两类:

**(a) 辅助文字(12-13px,基线口径内)**:`#8A8580` on `#FFFFFF`/`#F2F0EC`/`#FAF9F7` 实测
3.21-3.65,`#8A8884` on `#24272B` 实测 4.23。基线 §595-597 速查表已明确 `#8A8580` on `#FFFFFF`
= 3.5:1「**仅可用于辅助文字,不可用于正文**」—— 这些命中元素(`text-aux` / 表头 / 卡片小标签 /
徽章)确为 12-13px 辅助文字,按基线口径**不算缺陷**。axe 不做「辅助文字」豁免,故仍计为 serious。

**(b) 正文级(15-16px)不达标**:**首轮确为缺陷**,四处,见 §8-E;第二轮已全部修复,复验为零。

> 第二轮复扫(2026-09-11):亮色只剩上述 (a) 类,暗色 `/login` 与 `/` 已**零违规**,
> 全文**无正文级命中**。详见 §11.2。

### 5.3 断点恢复 / 会话过期回跳 / 操作栏恒定

| 项 | 结果 |
|---|---|
| 断点恢复 | ✓ 步 3 保存后换新页面 → 停在步 4,步 1 选中态保持 |
| 会话过期回跳 | ✓ 清 Cookie 访问 P03 → 带 `from` + `expired=1` 跳登录 → 重登回原页原步(6 → 6) |
| 操作栏位置恒定 | ✓ 六步操作栏 `gap` 实测 `{1:218, 2:276, 3:276, 4:52, 5:52, 6:331}`,跨步恒定;内容超一屏的步骤 gap 恒为 52(操作栏 `sticky bottom-0` 未随内容高度跳动) |

### 5.4 P01 双形态

| 形态 | 结果 |
|---|---|
| 无方案空态(临时置 `plans.is_active=false`) | ✓ 空态文案 + 引导入口出现、不展示摘要、「375px 无横向滚动」、页脚免责声明在;测后已恢复 active |
| 有方案摘要 | ✓ 摘要含模式名 / 版本 / 可投资额,Token 样例区已移除 |

### 5.5 埋点(AC-14)

| 事件 | 条数 |
|---|---|
| `page_view` | 44 |
| `questionnaire_start` | 1 |
| `questionnaire_step_completed` | 21 |
| `questionnaire_completed` | 3 |
| `plan_generated` | 4 |

`plan_generated` 4/4 条载荷含 `l1_mode`(如 `{"l1_mode":"fifty_30_20","plan_version":4}`)。

### 5.6 合规文案 grep(AC-13)

| 检查 | 结果 |
|---|---|
| 前端文案与组件中的产品 / 机构 / 代码词(基金 / 股票 / ETF / 券商 / 开户 / 申购 / 买入 / 机构名 …) | 0 命中 |
| 购买链接 / 外链 | 仅 `http://127.0.0.1:8080`(本机后端)与一处注释里的恶意外域**示例**,无对外链接 |
| 模式配置(`server/config/modes/**`) | 全为资产大类,无具体产品 |
| `dangerouslySetInnerHTML` | 0 命中 |

## 6. 基线 §16 代码审查清单

> 逐项打勾来源:`docs/base_line/基线-arch-Web域.md` §16。**未做的项如实留空并说明。**

### 16.1 架构合规

| 项 | 结果 |
|---|---|
| 分层(handler / service / repo),规则未散落 | ✓ `api/{mod,middleware,v1}` + `services` + `repos` + `domain` |
| 技术栈未擅自引入新依赖 | ✓ 依赖审计见 §7 |
| 项目结构含 README / `.env.example` / `.gitignore` / `.sqlx` | ✓ `.sqlx` 12 个文件已提交 |
| 表结构变更有 sqlx 迁移脚本 | ✓ 6 个迁移 |
| 每条 RULE-xxx 在 service 层唯一实现并标注编号 | ✓(代码内含 RULE 编号注释,如 `api/mod.rs:66`) |
| 无本地探测 / 安装中间件的代码或文档 | ✓ |
| 中间件地址与凭据全部走环境变量 | ✓ `Config::from_env` |
| `clippy -D warnings` 与 `biome check` 通过 | ✓ 见 §4 |

### 16.2 数据合规

| 项 | 结果 |
|---|---|
| 金额用 `i64` 分,无 float 参与金额运算 | ✓ `server/src` 内 `f64`/`f32` 零命中 |
| 数值字段标明单位 | ✓(`_cents` 后缀) |
| 列表接口已分页,无 N+1 | ✓ 单用户量级,列表接口为单次查询 |
| 索引已建立(外键 / 筛选 / 唯一) | ✓ 迁移内建唯一约束与索引 |
| 时间以 UTC 存储(`timestamptz`) | ✓ |

### 16.3 安全合规

| 项 | 结果 |
|---|---|
| 每个端点有服务端权限校验 | ✓ `requireSession` 中间件,非前端隐藏 |
| 密码 argon2 哈希 | ✓ `argon2 = "0.5"` |
| 无 SQL 字符串拼接(全部 `bind()`) | ✓ sqlx 编译期校验(`.sqlx` 已提交) |
| 用户输入已校验;无 `dangerouslySetInnerHTML` | ✓ 0 命中 |
| 无硬编码密钥 / 数据库 URL / 端口 | ✓ 仅 `127.0.0.1:8080` 默认值(可被环境变量覆盖,非密钥) |
| 日志中无敏感原文 | ✓ 种子脚本 `seed-user.sh` 只打印用户名、不打印口令 |
| 有登录失败限流 | ✓ 按用户名计数,1 分钟窗口 5 次,第 6 次 429 |

### 16.4 契约与体验合规

| 项 | 结果 |
|---|---|
| 统一响应信封 | ✓ |
| 错误码集中在 `ErrorCode`,前端有映射 | ✓ |
| 会话过期有保留现场 + 回跳 | ✓ 见 §5.3 |
| 前端调用走 `lib/api.ts`,类型来自 codegen | ✓ |
| 前端五态齐全(默认 / 空 / 加载 / 错误 / 成功) | ✓ P01 空态与摘要态、表单错误态、生成失败态均在 |
| 空态三要素(图标 + 说明 + 引导操作) | ✓ P01 空态 |
| 错误文案无技术黑话 | ✓ 「金额需大于 0」「用户名或密码不正确」等 |

### 16.5 设计合规

| 项 | 结果 |
|---|---|
| 无写死色值 / 字号 / 间距 / 圆角,全部引用 Token | ✓ Token 门禁通过 |
| shadcn 默认色板已替换为苑问色板 | ✓ |
| 触控热区 ≥ 44x44px | ✓ 首轮 ✗(§8-B),第二轮补齐并复验(§11) |
| 涨跌色红涨绿跌带 +/- | ✓ B 期无涨跌内容,不适用 |
| 暗色模式(如要求)已实现且对比度达标 | ✓ 首轮 ✗(§8-A / §8-E),第二轮补齐并复验(§11) |
| 核心页面 axe-core 扫描无严重 / 中等项 | **定性后收窄**:首轮 16 次扫描均报 `color-contrast` serious;第二轮复扫降到 12 次,且剩余命中**全部**是基线 §595-597 口径内的 12-13px 辅助文字,无正文级(§5.2 / §11.2) |
| 样式可被自动验证 | ✓ 溢出 / 触控 / 对比度三项均已脚本化 |

### 16.6 质量合规

| 项 | 结果 |
|---|---|
| 无 `println!` / `console.log` 残留 | ✓ 仅 `main.rs` 启动期配置错误提示用 `eprintln!`(非请求路径);前端 0 命中 |
| 无 `let _ =` / `unwrap_or_default()` 掩盖错误 | ✓ 仅 2 处 `let _ =`,均有明确意图(`dotenvy::dotenv()` 缺 `.env` 不算错误;测试内消除 unused) |
| handler 路径无 `unwrap()` / `expect()` | ✓ 命中全部落在 `#[cfg(test)]` 模块内 |
| 外部调用有超时与降级 | ✓ |
| `cargo test` 通过,关键规则有测试 | ✓ 94 passed |
| 有 `/health` 健康检查 | ✓ |
| 备份策略已落地并演练过恢复 | ✓ 见 §1 |

## 7. 依赖审计

### 7.1 cargo audit

`cargo audit`(0.22.2,扫描 277 crates):**1 个漏洞**。

| 项 | 值 |
|---|---|
| crate | `rsa 0.9.10` |
| 通告 | `RUSTSEC-2023-0071`(Marvin Attack) |
| 严重度 | medium(5.9) |
| 依赖路径 | `wise-wealth-server → jsonwebtoken 11.0.0 → rsa 0.9.10` |
| 修复 | **无**:`No fixed upgrade is available!` |

可达性评估:本项目只用 **HS256**(HMAC)签发与校验 JWT,不走 RSA 代码路径;但 `rsa` 会随
`jsonwebtoken` 默认特性进入依赖树。可评估关闭 `jsonwebtoken` 默认特性以收窄依赖面(见 §9-I4)。

### 7.2 npm audit

`npm audit`:**2 个 high**。

| 项 | 值 |
|---|---|
| 包 | `js-yaml 4.0.0 - 4.3.1` |
| 通告 | `GHSA-2883-xcg3-v3hh`(`maxTotalMergeKeys` 不限制 CPU 使用) |
| 路径 | `@redocly/openapi-core → js-yaml` |
| 性质 | 类型生成链的 **devDependency**,不进生产产物 |
| 修复 | 有:`npm audit fix` |

## 8. 未通过项与缺口

### 8-A 暗色手动三态切换未实现(AC-16 · FEATURE-009 · design-v2 §3.3)—— **已修复(2026-09-11,§11)**

现状:`globals.css` 只有 `@media (prefers-color-scheme: dark)` 跟随系统;无 `.dark` class 策略、
无 `@custom-variant dark`、无 localStorage 记忆、无切换按钮,`dark:` 工具类零使用。
即「跟随系统」能用,「手动切亮 / 切暗」不可用。

**这是规格切分的遗漏**:ticket 01-08 无任一条认领该项,而 prd-v1 §152 FEATURE-009 明确列了
「暗色手动切换」、§399/§401 与 design-v2 §3.3 均给了实现口径。

### 8-B 移动端触控目标未达 44px(AC-16 · 基线 §577/§429/§443 · design-v2 §43)—— **已修复(2026-09-11,§11)**

375px 实测(基线要求「触控热区 ≥ 44x44px」,design-v2 §43 给出移动断点 `h-11`(44px)):

| 页面 | 元素 | 实测高度 |
|---|---|---|
| `/login` | 两个输入框 | 40px |
| `/login` | 登录按钮 | 40px |
| `/questionnaire` | `← 返回首页` 链接 | **17px** |
| `/questionnaire` | `下一步 →` 按钮 | 40px |
| `/plan` | `重新生成` / `返回首页` | 40px |

### 8-C 顶部栏与登出按钮缺失(design-v2 §30)—— **已修复(2026-09-11,§11)**

`logoutAction`(`web/src/app/login/actions.ts:63-64`)已定义但**全仓库无调用点** ——
没有任何登出入口,design-v2 §30 要求的「顶部栏(暗色切换按钮 + 登出按钮)」整体缺失。

### 8-D 文案 bug:「资金久期在 1 年内 内」

`server/src/domain/l2.rs:77` 的模板自带「 内」,而 `Horizon::Within1y` 的 label 已是「1 年内」,
拼接后成了「资金久期在 1 年内 内,短期要用的钱不进入权益类与债券类」。一行可修。

### 8-E 对比度:4 处正文级(15-16px)文字不达标(基线 §16.5)—— **已修复(2026-09-11,§11)**

| 主题 | 元素 | 前景 / 背景 | 实测 | 字号 |
|---|---|---|---|---|
| 亮 | `.text-danger`(生成失败 / 严重提示) | `#C0553F` / `#F2F0EC` | **4.0:1** | 16px |
| 暗 | accent 按钮 / 链接文字 | `#6B8B77` / `#243029` | **3.65:1** | 16px |
| 暗 | accent 按钮 hover | `#6B8B77` / `#1E2024` | **4.33:1** | 15px |
| 暗 | `.text-danger` | `#D4694F` / `#24272B` | **4.23:1** | 16px |

均低于正文 4.5:1 要求。**第二轮已修**:不动基线色值,改**语义 Token 换用** —— 新增
`--color-action`(亮 `#4A6B57` / 暗 `#8AA894`)承载 accent 按钮与链接文字的前景;徽章(含问卷
「可信度」徽章)前景改 `accent-foreground`;严重提示条底色 `bg-subtle` → `bg-card`(实测
4.00/4.24 → 4.55/4.61)。复验为零(§11.2)。其余 axe 命中为 12-13px 辅助文字,按基线口径不算缺陷(§5.2)。

### 8-F 依赖审计

见 §7:`rsa` 1 个 medium(**上游无修复版本**)、`js-yaml` 2 个 high(dev 依赖,可自动修)。

## 9. 遗留清单(结项登记)

| 编号 | 事项 | 性质 | 处置 |
|---|---|---|---|
| I-1 | §8-A 暗色手动三态切换未实现 | 功能缺口(AC-16 首轮不通过主因) | **已修并复验**(2026-09-11,§11) |
| I-2 | §8-B 触控 ≥44px 未达标 | 功能缺口(AC-16 首轮不通过主因;A 期遗留 OUT 项) | **已修并复验**(2026-09-11,§11) |
| I-3 | §8-C 顶部栏与登出按钮缺失 | 功能缺口(design-v2 §30) | **已修并复验**(2026-09-11,§11) |
| I-4 | §8-D 文案「1 年内 内」 | 一行 bug | **已修**(`server/src/domain/l2.rs:77`) |
| I-5 | §8-E 4 处正文级对比度不达标 | 设计 Token 缺陷 | **已修**(语义 Token 换用,未动基线色值);残留 disabled 按钮一项按 WCAG 1.4.3 豁免登记,见 §11.4 |
| I-6 | §7.1 `rsa` medium,无修复版本 | 依赖风险 | 关注上游;可评估收窄 `jsonwebtoken` 默认特性 |
| I-7 | §7.2 `js-yaml` high(dev) | 依赖风险 | `npm audit fix` |
| I-8 | 收入不足场景文案「按每月 ¥0.00 的节奏,约 0 个月补齐」有歧义 | 文案打磨 | 顺延,低优先 |
| I-9 | `test-report-b.md` §1.2 的 `restore-drill.sh` **prod 分支未实跑** | 覆盖缺口 | 正式库首次使用前补跑 |
| I-10 | RISK-B-1:TLS 与独立正式库顺延(OUT-007) | 已登记风险 | 触发条件:对外 / 部署前必须解决 |
| I-11 | 苑问用**真实数字**完整走一遍问卷 → 方案(DoD 验收动作) | 待本人执行 | 需用户本人操作 |
| I-12 | 非 Chromium 浏览器(Safari / Firefox)、真机移动端未走查 | 覆盖缺口 | 本期未列入,对外前评估 |

## 10. 结论

- **功能链路成立**:登录 → 问卷 → 方案 → 版本化 → 备份恢复,端到端可跑通;17 条 AC **全部通过**
  (AC-16 首轮不通过,三处缺口补齐后于第二轮复验通过,见 §11;AC-9 的文案瑕疵已修,见 §8-D)。
- **自动化水位**:`clippy -D warnings` + 94 单测 + `biome` + `tsc` + Token 门禁全绿;
  溢出 / 触控 / 对比度三项走查已脚本化,可复跑。
- **B 期验收成立**:AC-16 补齐后,本期无阻断性未通过项。剩余为登记型遗留(§9 I-6 / I-7 / I-9 /
  I-10 / I-12)与一项需苑问本人执行的 DoD 动作(§9 I-11)。
- **本报告不含「未做的假装做了」**:prod 分支恢复演练(§1.2 末)、真实数字走查(§9-I11)、
  非 Chromium 浏览器(§9-I12)均如实标注为未覆盖。

## 11. 第二轮复验:AC-16 三处缺口补齐(2026-09-11)

首轮走查的三处缺口(§8-A / §8-B / §8-C)与 4 处正文级对比度(§8-E)于同日补齐,并以同一套脚本
复验。环境与首轮一致(dev 库 + 本机服务;Chromium headless / Playwright 1.63.0)。

### 11.1 处置清单

| 缺口 | 处置 | 落点 |
|---|---|---|
| §8-A 暗色手动三态 | 三态循环 `system → light → dark`(`.dark` class 策略 + `localStorage` 键 `ww-theme` + 首帧内联防闪烁脚本),与 A 期 `@media (prefers-color-scheme: dark)` 并存 | `web/src/components/theme-toggle.tsx`(新)、`web/src/app/globals.css`、`web/src/app/layout.tsx` |
| §8-B 触控 ≥44px | 移动断点 `h-11`(44px)、桌面回 `h-10` | `web/src/components/ui/button.tsx`、`login-form.tsx`、`questionnaire/wizard.tsx`、`top-bar.tsx` |
| §8-C 顶部栏与登出 | 新增 `TopBar`(品牌回 P01 + 主题切换 + 登出 `form action={logoutAction}`);未登录时不渲染登出 | `web/src/components/top-bar.tsx`(新)、`web/src/app/layout.tsx` |
| §8-D 文案「1 年内 内」 | 模板去掉多余「 内」 | `server/src/domain/l2.rs:77` |
| §8-E 4 处正文级对比度 | **语义 Token 换用,不动基线色值**:新增 `--color-action`(亮 `#4A6B57` / 暗 `#8AA894`);accent 按钮与链接文字改用 `text-action`;`Badge` 与问卷可信度徽章前景改用 `accent-foreground`;严重提示条底色 `bg-subtle` → `bg-card` | `globals.css`、`button.tsx`、`badge.tsx`、`plan-view.tsx`、`wizard.tsx` |

Token 策略:`--dark-*` 值**单点定义**,`@media (prefers-color-scheme: dark)`(经 `:root:not(.light)`
守卫)与 `.dark` class 两条路径只做机械映射,避免两份值漂移。

### 11.2 复验结果(全绿)

| 段 | 覆盖 | 结果 |
|---|---|---|
| A | 系统暗 + 手动三态循环(1280) | ✓ 24/24 |
| B | 系统亮 + 手动切暗(375) | ✓ 4/4 |
| D | 顶部栏与登出(显隐 / 跳转 / Cookie 清除 / 登出后访问业务页回跳) | ✓ 6/6 |
| E | 375px 触控热区 ≥44px(4 页;`button/a/input/select/textarea/[role=button]`) | ✓ 零命中 |
| F | 溢出扫描 4 档 × 4 页 × 亮暗 = 32 组合 | ✓ 全 0px |
| G | 对比度三套(媒体亮 / 媒体暗 / 手动暗)× 4 页 | ✓ 正文级零违规 |

> 判据说明:G 段脚本**不做**「辅助文字」与「inactive 控件」豁免,故其退出码仍会列出 §11.4 的
> 已定性项(亮 11 / 暗 4),**不等于复验不通过** —— 判据是**正文级(15-16px)是否命中**,该项已为零。
> 同理 axe 仍计 serious(§11.2 末)。

- **两条主题路径同值**:手动 `.dark` 与系统偏好暗的违规集合逐一相同(G4 断言通过)—— 证明
  `--dark-*` 单点定义没有漂移。
- **正文级(15-16px)对比度违规清零**:首轮 §8-E 的 4 处全部消失。
- axe-core 复扫:严重项从**首轮 16 次扫描全报**降到 **12 次**,且剩余命中经逐节点核对
  (`failureSummary` 打印 `target` + 前景/背景/字号)**全部**是基线 §595-597 口径内的 12-13px 辅助文字:

| 主题 | 命中 | 前景 / 背景 | 实测 | 字号 |
|---|---|---|---|---|
| 亮 | `text-aux` / 表头 / 卡片小标签 | `#8A8580` / `#FFFFFF`·`#F2F0EC`·`#FAF9F7` | 3.21-3.65:1 | 12-13px |
| 暗 | `text-aux` / 表头 | `#8A8884` / `#24272B` | 4.23:1 | 12-13px |

暗色 `/login` 与 `/` 已**零违规**。axe 不做「辅助文字」豁免,故仍计 serious(与 §5.2 定性一致)。

### 11.3 复验中发现并修掉的两处新缺口

1. **`TopBar` 品牌链接触控热区不足**:新增组件时漏了 `min-h` → 375px 实测 72×25px。已改为
   `inline-flex min-h-11 items-center sm:min-h-10`;E 段复跑零命中。
2. **问卷「可信度」徽章**(如「已考证」)漏改:`badge.tsx` 改了但 `wizard.tsx` 里的手写 `span`
   未改,暗色下 `#6B8B77` on `#243029` 仅 3.65:1。已改用 `text-accent-foreground`,暗色违规项
   随之从 5 降到 4。

### 11.4 登记遗留(不改,已定性)

| 项 | 实测 | 定性 |
|---|---|---|
| disabled「生成方案」按钮(亮 2.76:1 / 暗 1.77:1) | 前景 `#B5B0AA` / `#5E5C59` | **WCAG 1.4.3 豁免 inactive 控件**,不修。axe 自身也不报此项(其 `color-contrast` 规则排除 disabled 表单元素),与人工判定一致。design-v2 §173 已建议填充型主按钮用 `--color-primary-deep`,属基线 Token 回填,不混入本轮 |
| 12-13px 辅助文字 3.21-4.23:1 | 见 §11.2 表 | 基线 §595-597 口径内,不算缺陷 |

### 11.5 未覆盖(与首轮一致,不假装做了)

- 苑问用**真实数字**完整走一遍问卷 → 方案(§9-I11,DoD 动作,需本人执行)
- `restore-drill.sh` 的 prod 分支未实跑(§9-I9)
- 非 Chromium 浏览器(Safari / Firefox)、真机移动端未走查(§9-I12)
- 未做人工读屏验证(仅 axe-core 自动扫描)
