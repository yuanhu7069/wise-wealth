# 环境与凭证（基线红线：禁止提交 .env）
.env*
!.env.example

# 构建产物
target/
node_modules/
.next/

# 数据库备份(arch-v2 §7:本地 backups/ + 异地各一份;只在本机留存,不入库)
backups/

# 日志与临时文件
*.log
.DS_Store
.reasonix/
reasonix.toml
.gstack/

# graphify 生成物(可由 /graphify 重建,不入库)
graphify-out/
