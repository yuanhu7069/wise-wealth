# 智策理财 (wise-wealth)

双层理财决策工具：免费 Guide 层回答「怎么做」，Plus 层回答「为什么是这个数、换个条件会怎样」。不触碰资金，只输出建议与模拟。

## Language

### 产品分层

**Guide 层**:
免费、默认入口的一层；给到的是一张可照做的方案表。
_Avoid_: 起步模式、免费层、外层

**Plus 层**:
进阶的一层；展开方案数字的推理链，假设参数可见、可调。
_Avoid_: 推演模式、Lab、内层

### 模式体系

**模式 (Mode)**:
一种有出处、有可信度评级的资金安排方法，配置化存在于模式库中。
_Avoid_: 策略、模板

**分账模式 (L1)**:
回答「钱进来先切成几堆」的模式层（如四账户、50/30/20）。

**配置模式 (L2)**:
回答「投资的钱装什么大类资产、各占多少」的模式层（如永久组合、60/40、核心-卫星）。

**执行规则 (L3)**:
回答「怎么买、什么时候调」的模式层（定投节奏、再平衡触发）。

**可信度 (Credibility)**:
模式出处的三级评级：verified（已考证）/ disputed（存疑）/ caution（谨慎）。

**出处 (Source)**:
模式来源的说明（出版物 / 机构 / 流传史）；与可信度评级并列的模式必备元数据，缺失则模式不合法。存疑模式的出处含辟谣说明。
_Avoid_: 参考文献、考据（考据专区是知识库的概念）

**手动选择 (Manual pick)**:
从模式库直接指定某个模式生成方案的行为；与问卷自动匹配并列的模式触达路径。存疑模式只能手动选择，不能被自动匹配主推。
_Avoid_: 切换模式（那是追踪模块 T5 的概念）、自选

### 分账语义

**桶 (Bucket)**:
L1 模式中资金的一个流向目的地；一个模式由若干桶构成。

**入流 (Inflow)**:
每月参与分账的税后收入总额。

**余量桶**:
承接其余桶求解完毕后剩余份额的桶；一个模式至多一个（B 期为投资桶）。
_Avoid_: 双 remainder、剩余桶

**规则桶**:
月转入额由规则计算而非固定比例的桶（B 期仅备用桶）。

**必要月支出**:
失去收入时仍须支付的月度开销；由模式声明哪些桶计入（四账户 = 工资桶 + 消费桶；50/30/20 = 必要桶）。

**应急目标**:
应急月数 × 必要月支出。

**缺口 (Gap)**:
应急目标 − 现有存款。

**达标 (Met)**:
现有存款 ≥ 应急目标的状态；达标后规则桶月转入为 0，份额落入余量桶。

**safety_first**:
资源不足以覆盖全部桶时的让位次序：应急金 > 保障 > 投资。

### 问卷与档案

**档案 (Profile)**:
一名用户的问卷答案与财务数字的持久化记录。

**久期 (Horizon)**:
问卷第 1 题「这笔钱几年后要用」的答案；唯一直接决定权益上限的变量。

**回撤反应**:
问卷第 2 题「100 万跌到 70 万的第一反应」的答案；行为指标，优先于风险自评。

**收入稳定性**:
问卷第 3 题答案；决定应急月数。

### 方案

**方案 (Plan)**:
对一份档案运行一个 L1 模式（含自动匹配的 L2/L3）得到的完整输出；重新生成产生新版本，历史版本保留。
_Avoid_: 报告、计划书

### 追踪

**月度快照 (Snapshot)**:
对当前 active 方案全部桶余额做的一次月底记录；一个自然月至多一条，重提交即覆盖当月。
_Avoid_: 逐笔记账、打卡记录、报表

**跳过月**:
没有快照的自然月；不惩罚、不清零已有累计。
_Avoid_: 缺卡、断签

**本月特殊 (Special month)**:
快照上的豁免标记：该快照不触发偏离提示，也不作为下一次环比的基准。
_Avoid_: 跳过（与跳过月混用）

**偏离 (Deviation)**:
相邻两条快照中同一桶余额的环比变化超过阈值。
_Avoid_: 异常、错误、波动

**已坚持月数**:
有快照记录的自然月累计数；衡量的是坚持的总量，不是连续性。
_Avoid_: 连续打卡天数

**距离感进度条**:
以「还差 X 个月」「已坚持 Y 个月」这类具象距离呈现进度的要求；不展示抽象完成度百分比。
_Avoid_: 完成度百分比、进度百分比

### 设计治理（Open Design 生成型）

**生成型基线**:
界面由 Open Design MCP 生成的治理方式；设计真源是"生成动作"而非文档，落地后真源移入代码库。基线见 `docs/base_line/基线-design-OpenDesign-Web域.md`。
_Avoid_: 参考设计型、改造件流程

**视觉基准 (Design System)**:
生成前固定的 design direction / designSystemId；没有它就生成等于抽奖，T1 无法验收。
_Avoid_: 主题、皮肤

**草稿 (Draft)**:
生成产出、尚未过 T1 验收的 artifact；不得据此写业务代码。
_Avoid_: 交付物、成品

**固化**:
把过闸草稿搬进 Git 的动作：tokens 与组件进代码、产物副本进 `docs/design/`、元数据进生成记录。漏了这步 = 设计从未存在。
_Avoid_: 保存、导出

**落地**:
按固化产物实现进代码库；落地后真源 = 代码库，改动直接改代码，不回炉生成。
_Avoid_: 迁移、还原

**三道闸门 (T0/T1/T2)**:
T0 参数闸门（生成前）→ T1 草稿验收（一致性 + 完整性六项）→ T2 落地验收（对固化产物的偏离须能在调整清单找到）。全部是校验，不含审美判断。
_Avoid_: 验收流程（泛称）

**调整清单**:
落地时对生成原值的显式偏离表（原值 / 项目值 / 理由）；清单外一律照产物执行，AI 不得自行发挥。
_Avoid_: 本地化配置、自定义

**选择 vs 校验**:
好不好看 = 选择（人做并排对比，不进验收清单）；对不对、全不全 = 校验（三道闸门）。两者不得混谈。
