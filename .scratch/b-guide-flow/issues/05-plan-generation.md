# 05: 方案生成与方案页

**What to build:** 选定模式后点「生成方案」，得到一页五段式方案：分配总览（每个账户每月转入多少）、投资账户内部配置（大类比例）、执行规则（定投与再平衡）、落地建议（开几张卡怎么分）、风险提示。金额与引擎输出逐项一致。

**Blocked by:** 01（引擎）、04（需要已选定的模式）

**Status:** done（2026-09-10）

**完成记录**：后端（`0004_plans` 迁移 + `plan_service` 编排 + `/plans`、`/plans/active` 端点）+ 前端（P04 五段纯 server 组件；页脚与声明文案经 `SiteFooterShell` / `disclaimer-copy.ts` 收敛，以支持 RULE-020 的按页承载位置）。**端到端实测（curl，金例 A）通过**：登录 → 分步存问卷 → 生成 → 读回，四个桶金额 4,500 / 3,600 / 3,100 / 800 与引擎输出**逐项一致**，L2 = 60/40，应急 12 个月 / 已覆盖约 3.0 个月 / 约 24 个月补齐，生成快照 = 读回快照；未带 Cookie 生成 → 401。后端 86 项单测全绿、前端 `tsc --noEmit` 通过。**遗留**：方案页 375px 与桌面双端、暗色三态的浏览器人工走查未做；「重新生成」按钮待 06（版本化）落地。

**切分理由：** 原本与「版本化重算 + 首页改造」合为一张 ticket，实施前拆开，因为合起来含两张表、生成接口、五段页面、首页改造与版本化，超出一个上下文窗口。拆后本 ticket 是**最短可演示路径**（生成一次就能看到方案），06 是在此之上的增量。

- [ ] 生成请求返回并持久化方案（含桶明细与 L2 大类配置）
- [ ] 方案页五段齐备，且金例 A 的四个桶金额与引擎输出**逐项一致**（4,500 / 3,600 / 3,100 / 800）
- [ ] 收入不足时展示缺口提示，任何金额不为负
- [ ] 应急金已覆盖时展示达标提示与「投资账户已有家底」金额
- [ ] 页面标注模式名、版本号与生成时间
- [ ] 金额右对齐、千分位、两位小数、等宽数字
- [ ] 生成失败有可重试的错误态，已答问卷内容不丢
- [ ] 全页不出现任何具体金融产品名称、代码、管理机构或购买链接
- [ ] 风险提示与假设可见性符合 prd-v1 RULE-020 与合规红线
