# 09: 验收走查与测试报告

**What to build:** 全流程人工走查并产出 B 期测试报告，作为本期验收依据：双端（桌面与 375px 移动）与双主题（亮/暗）走通「登录 → 问卷 → 方案」，断点恢复与会话过期回跳实测，四档宽度溢出扫描零命中；苑问用自己的真实数字完整走一遍。

**Blocked by:** 06、07、08（需要功能全量就位、埋点可查、备份演练完成）

**Status:** ready-for-human（2026-09-11，第二轮复验后）—— 走查已跑完并产出报告；AC-16 首轮的三处缺口已补齐并复验通过（见报告 §11），**17 条 AC 全部通过**；仅剩「苑问用真实数字走一遍」这一 DoD 动作需本人执行。

**切分理由：** 作为收尾 ticket 而非把走查分散到各 ticket：本项目的验收节奏是**整期一次**（A 期即如此），分散走查会在功能仍在变动时反复重跑。详细清单与 A 期同构，便于对比两期的质量水位。

**执行记录（2026-09-11）**：走查在 dev 库 + 本机服务上完成（代码版本 `247b986` + 本轮 README 改动），
浏览器 Chromium headless（Playwright 1.63.0），视口 320/375/768/1280 × 亮/暗双主题。
完整证据、AC 回链、基线 §16 清单、依赖审计、遗留清单见 **`docs/b/test-report-b.md`**。走查脚本为一次性脚本，
跑在 `/tmp` 下、**不进仓库**（要复现按报告 §2 重建环境即可，`scripts/check.sh` 是常驻自动化门禁）。

**结论：17 条 AC 全部通过。**（AC-16 首轮不通过；三处缺口补齐后第二轮复验通过，AC-9 的文案瑕疵亦已修 —— 见报告 §11。）

**AC-16 的三处缺口（首轮发现，均已补齐并复验通过 —— 详见报告 §11）：**

| 缺口 | 首轮现状 | 处置 | 规格依据 |
|---|---|---|---|
| 暗色手动三态切换未实现 | 只有 `@media (prefers-color-scheme: dark)`；无 `.dark` class / `@custom-variant dark` / localStorage / 切换按钮 | **已修**：三态循环 `system → light → dark`（class 策略 + `localStorage` + 首帧防闪烁） | FEATURE-009、prd §399/§401、design-v2 §3.3、AC-16 |
| 移动端触控目标未达 44px | 375px 实测：输入框与按钮 h=40、`← 返回首页` h=17 | **已修**：移动断点 `h-11`(44px)、桌面回 `h-10`；复验零命中 | 基线 §577/§429/§443、design-v2 §43、AC-16 |
| 顶部栏与登出按钮缺失 | `logoutAction` 已定义但**全仓库无调用点**，没有任何登出入口 | **已修**：新增 `TopBar`（品牌回 P01 + 主题切换 + 登出） | design-v2 §30 |

**这是规格切分的遗漏**：ticket 01-08 无任一条认领过「顶部栏 / 暗色切换 / 登出 / 触控 44px」，
而 prd-v1 §152 FEATURE-009 明确列了前三项。故「ticket 全 done」不等于「需求全实现」。

**另有两处非 AC-16 的发现（均已修）**：①文案 bug「资金久期在 1 年内 内」
（`server/src/domain/l2.rs:77`，模板自带「 内」而 label 已是「1 年内」）—— 已去掉多余「 内」；
②4 处**正文级**（15-16px）文字对比度不达标（亮色 `.text-danger` 4.0:1；暗色 accent 按钮文字
3.65:1、hover 4.33:1、暗色 `.text-danger` 4.23:1）—— 已用语义 Token 换用修复（新增 `--color-action`，
未动基线色值），复验正文级零违规。其余 axe 命中的 12-13px 辅助文字按基线 §595-597 口径（该色
「仅可用于辅助文字」）不算缺陷 —— 详见报告 §5.2 / §8-E / §11.2。

**依赖审计**：`cargo audit` 1 个 medium（`rsa 0.9.10`，Marvin Attack，**上游无修复版本**，经
`jsonwebtoken` 默认特性引入；本项目只用 HS256 不走 RSA 路径）；`npm audit` 2 个 high
（`js-yaml`，属类型生成链 devDependency，`npm audit fix` 可解）。

**未覆盖项（不假装做了）**：①苑问用真实数字走一遍（DoD 动作，需本人）；②`restore-drill.sh`
的 prod 分支未实跑（见报告 §1.2）；③非 Chromium 浏览器与真机移动端未走查；④AC-16 之外的
axe-core 严重项已逐条定性，但未做人工读屏验证。

- [x] 320／375／768／1280 四档 × 四页面溢出扫描零命中（`scrollWidth/scrollHeight` vs `clientWidth/clientHeight`）—— 亮暗双主题共 32 组合全 0px
- [x] 亮色与暗色双主题对比度达标（正文 ≥ 4.5:1、大字与图形 ≥ 3:1）—— 首轮 4 处正文级不达标，修复后复验正文级零违规；残留 12-13px 辅助文字按基线 §595-597 口径不算缺陷（报告 §11.2 / §11.4）
- [x] 断点恢复实测：答到步 3 关闭页面 → 重进停在步 4，步 1-3 答案保留
- [x] 会话过期回跳实测：过期后访问问卷 → 登录 → 回到原步（实测 6 → 6，内容完整）
- [x] 向导操作栏在六步之间位置恒定（不随步骤内容高度跳动）
- [ ] 苑问用真实数字完整走一遍问卷 → 方案（DoD 验收动作）—— **待本人执行**
- [x] `test-report-b.md` 产出：AC 回链、基线 §16 审查清单、依赖审计（cargo audit / npm audit）、遗留清单
- [ ] prd-v1.md 状态流转与本期文档收尾（design-v2 变更记录、遗留项登记）—— AC-16 结论已出，收尾时一并流转
