# customInstructions · g-knowledge（2026-09-25 落盘）

> 本文件是生成提示词的一部分（基线 §2.1），修改须记入生成记录。
> 结构 = D 期定稿全文（od-redesign @ 01e074a，逐字沿用）+ G 期追加节（P07/P08 通用）。

## 承 D 期定稿（逐字沿用）

- 语言：界面文案一律简体中文，沿用现有页面真实文案与信息架构；不新造功能、不增删字段
- 主题：亮 / 暗两套都要（现有三态切换 跟随系统→亮→暗 的行为保留）
- 五态：每页齐备 空 / 加载 / 错误 / 空结果 / 正常，不接受只给正常态
- 极端数据：金额超长（¥12,345,678.90）、标题超长截断、列表 0 条与海量、负数
- 数值：等宽数字、千分位、右对齐
- 中文渲染：标题不用负字距、细体加重一档，正文行高放宽
- 无障碍：正文对比度 ≥ 4.5:1，焦点可见、键盘可达
- 自包含：不引用外部 CDN / 在线字体；中文回退 PingFang SC / Microsoft YaHei
- 移动端：375px 可用，触控目标 ≥ 44px
- 边界：只设计视觉与布局；接口、数据流、业务字段一律不动

## G 期追加（P07 知识库页 + P08 对比页 · 两页生成通用）

- 视觉一致性：两页均为既有六页体系里的**新页**，token 只用既有契约值，不发明任何新色值/字阶/间距；组件形态对齐既有分区卡、提示条、pill 按钮语汇；文章排版以「分区卡 + 段落」承载，不用 markdown 样式
- **P07 知识库页（/knowledge）**：页头「知识库」+ 一句说明「每种方法的来龙去脉，都摊开给你看」；四个板块分区（SectionTag 分隔，纵排）：
  1. **模式解读**：4 张文章卡（四账户理财法 / 50/30/20 / 四笔钱 / 标准普尔象限），每卡 = 标题 + 所属模式可信度徽章（沿用既有三色语义）+ 一句导语 + 「阅读」按钮
  2. **出处考据**：1 张卡（标准普尔象限考据），存疑橙徽章 + 「辟谣深读」导语
  3. **理财百科**：8 个词条卡（再平衡 / 费率 / 应急金 / 复利 / 通胀 / 资产大类 / 定投 / 回撤），小卡网格（桌面 2 列 / 375px 单列）
  4. **不可落地专区**：3 张卡（生命周期杠杆版 / 美林时钟 / 耶鲁模式），每卡带「仅供理解，不建议照搬」灰色警示徽章
  - 文章阅读态（同页展开或详情区，两态都给）：标题 + 可信度徽章 + sections（小标题 + 段落）；**「局限性」「不建议照搬」类内容必须视觉上不弱化**（正常排版，不做灰字弱化）
- **P08 对比页（/modes/compare）**：页头「模式对比」+ 一句说明「最多选 3 个，按你的档案试算」；参数对比表格：
  - 行 = 对比维度（可信度徽章 / 一句话理念 / 适合人群 / 桶结构（桶名+口径）/ 必要月支出口径 / **按你的档案每月转入**）；列 = 模式（2-3 列）
  - 金额列等宽千分位右对齐；不可行模式列显示警示短语（如「收入不足」）而非编造金额
  - 顶部「返回模式库」链接；表格 375px 横向可滑动，首列（维度名）固定
- 极端数据：文章标题超长截断、sections 空段不渲染、对比仅 2 模式（列自适应）、金额超长与负数红字
- 五态：列表骨架 / 错误条 + 重试 / 内容为空防御态 / 正常态 / 对比页加载中
- 边界：只设计视觉与布局；接口、数据流、业务字段一律不动；不设计分位数图表（Plus 范围）
