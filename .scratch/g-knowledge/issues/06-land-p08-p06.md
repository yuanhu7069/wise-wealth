# 06 · P08 落地 + P06 增量：表格 + 勾选/链接 + 回归

Status: open
Labels: ready-for-agent

> g-plan T6。前置：05 完成。规格：arch-g.md §1、§3（RULE-041/042/043）；prd-g.md §8。**P06 增量为代码级，不回炉生成**。

## 步骤

1. 新建 `web/src/app/modes/compare/`：page（server 壳，解析 URL `?modes=` + 调 preview，单请求渲染）+ 表格视图（维度成行/模式成列；金额列等宽千分位右对齐；不可行列警示短语；0 勾选 → 引导回 P06；375px 横滑首列固定）；`/modes/compare` 进 requireSession 链 + `trackPageView("p08")`
2. P06 增量（mode-card.tsx 代码级）：每卡「加入对比」勾选（checkbox 语义，≤3 超限提示 ERR-G-03）+ 底部「开始对比」按钮（激活条件 ≥2，URL 承载勾选态）+「阅读完整解读」链接（跳 `/knowledge?id=ki-<mode_id>`）
3. 调整清单落 spec.md §4；T2 验收清单外零偏离
4. 回归：P06 既有功能（列表/详情/CTA 生成）不受影响
5. 走查 AC-5/6/7/9/10/11/14 + AC-6 抽样对账（试算金额 vs 单独生成，2 模式）截图存 shots/

## DoD

- `check.sh` 全绿
- AC-5/6/7/9/10/11/14 走查过；对账一致（AC-6）
- P06 回归无破坏；金额不入埋点日志核对
