# customInstructions · f-modes（2026-09-25 落盘）

> 本文件是生成提示词的一部分（基线 §2.1），修改须记入生成记录。
> 结构 = D 期定稿全文（od-redesign @ 01e074a，逐字沿用）+ F 期 P06 追加节。

## 承 D 期定稿（逐字沿用）

- 语言：界面文案一律简体中文，沿用现有页面真实文案与信息架构；不新造功能、不增删字段
- 主题：亮 / 暗两套都要（现有三态切换 跟随系统→亮→暗 的行为保留）
- 五态：每页齐备 空 / 加载 / 错误 / 空结果 / 正常，不接受只给正常态
- 极端数据：金额超长（¥12,345,678.90）、标题超长截断、列表 0 条与海量、负数
- 数值：等宽数字、千分位、右对齐
- 中文渲染：标题不用负字距、细体加重一档，正文行高放宽
- 无障碍：正文对比度 ≥ 4.5:1，焦点可见、键盘可达
- 自包含：不引用外部 CDN / 在线字体；中文回退 PingFang SC / Microsoft YaHei
- 移动端：375px 可用，触控目标 ≥ 44px
- 边界：只设计视觉与布局；接口、数据流、业务字段一律不动

## F 期追加（P06 模式库页 · 新页生成专用）

- 视觉一致性：P06 是既有五页体系里的**新页**，token 只用既有契约值，不发明任何新色值/字阶/间距；组件形态对齐既有分区卡、提示条、pill 按钮语汇
- 页面结构（页头 + 卡片列表纵排，桌面可双列、375px 单列）：
  1. **页头**：标题「模式库」+ 一句说明（「每种分账方法都有出处与可信度评级」）
  2. **模式卡列表**（5 张样例，含 1 张存疑卡对照）：每卡 = 模式名 + 一句话理念 + 可信度徽章（绿「已考证」/ 橙「存疑」/ 黄「谨慎」，**徽章必须带文字，不只靠颜色**）+ 适合人群 pill（0-2 个）+「展开详情」文字按钮；推荐模式另带「推荐」徽章（全列表至多 1 张）
  3. **展开详情**（收起 ↔ 展开两态都要给）：出处块（来源说明，存疑卡含辟谣一句）+ 桶概览行（桶名 / 用途 / 月转入口径，如「每月收入的 30%」「按应急金节奏划入」）+ 主按钮「用此模式生成方案」
- **存疑卡样例（必须给）**：「标准普尔象限」——徽章「存疑」（橙色），出处 = 「网传『标准普尔家庭资产象限图』；标准普尔公司从未发布，系 2011 年前后国内保险业自创」；警示由徽章与文案承载，**不做整卡红色遮蔽**，CTA 照常可用
- 五态：列表骨架 / 防御空态（「模式库暂时为空」）/ 错误条 + 重试 / CTA 生成中 loading（禁重复点击）/ 正常态
- 极端数据：模式名与出处超长（截断 + 展开后全文）、tagline 缺省（可无理念行）、5 卡满列表；**本页无金额输入**
- 无障碍：徽章三色语义沿 design system（绿/橙/黄），焦点环、键盘展开/收起可达
- 边界：只设计视觉与布局；接口、数据流、业务字段一律不动
